

____________________________________________________

Gates i Javascript:

a og b = inputs

AND = a && b
NAND = +!(a && b)
OR = a || b
NOR = +!(a || b)
NOT = +!a
XOR = +(a !== b);
XNOR = +!(a !== b)

____________________________________________________